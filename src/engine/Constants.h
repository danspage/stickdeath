#pragma once

namespace GameEngine
{
    /// @brief A small value used for floating point comparisons to avoid precision issues.
    const inline float EPSILON = 0.0001;

    /// @brief The acceleration due to gravity.
    inline const float GRAVITY = -25;
}